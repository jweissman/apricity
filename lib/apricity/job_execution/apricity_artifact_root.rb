# frozen_string_literal: true

# can be removed i think
# module Apricity
#   module JobExecution
#     APRICITY_ARTIFACT_ROOT = ENV.fetch("APRICITY_ARTIFACT_ROOT") do
#       File.directory?(WORKING_DIR) ? File.join(WORKING_DIR, ".apricity") : File.join(Dir.pwd, ".apricity")
#     end
#   end
# end
